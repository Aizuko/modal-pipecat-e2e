[project]
name = "voice-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pipecat-ai[webrtc,openai,silero,noisereduce,soundfile]==0.0.92",
    "websockets",
    "loguru",
    "fastapi",
    "modal",
    "openai",
]
